# Birthday Gift Generator.

## pnpm 환경에서 Supabase CLI를 이용해 타입 파일 자동 생성

```bash
# 로그인
pnpm dlx supabase login

# 타입 파일 자동 생성
pnpm dlx supabase gen types typescript --project-id [PROJECT_ID] > types/database.types.ts
```
